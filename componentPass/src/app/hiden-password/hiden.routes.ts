import { Routes } from '@angular/router';
import { HidenPasswordComponent } from './hiden-password.component';

export const provaRoutes: Routes = [
  { path: 'hiden-password', component: HidenPasswordComponent },
];
